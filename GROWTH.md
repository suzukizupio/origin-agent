# origin の成長記録

遠い目標は、自分で調べ、コードを書き、経験から改善できるAI。
今は小さな「できた」を一つずつ確かめて、育つ過程を楽しみます。

## 今の到達点

| 段階 | 状態 | 確かめ方 |
|---|---|---|
| 挨拶・簡単な会話 | 実機で確認 | `npm start` で話しかける |
| 同じ会話の内容を参照 | 名前の確認に成功した例あり | `npm run check:live` |
| キーワードからネットを検索 | 実機で確認 | `/search TypeScript 公式 とは` |
| 検索結果から日本語で回答し、URLを提示 | 実機で確認 | `npm run check:live` |
| ファイルを読む・小さく編集する | 設定値1つの変更を実機で確認 | `npm run check:code` |
| 説明されたバグを直す・関数を足す | 3Bで 3/3・2/3（v0.7） | `npm run eval -- --only fix-described-bug,add-function` |
| 落ちているテストの原因を推理して直す | 3Bは以前0/3、今回も0/1。7Bは別の2課題で各1/1（再現性未確認） | `npm run eval -- --only fix-failing-test` |
| 終了後も呼び方・感想を覚える | 保存・再利用・削除を実装 | `/remember`・`/feedback`、自動テスト |
| 同じ課題を繰り返し採点する | 実装・3Bと7Bの比較記録あり | `npm run eval` |
| 失敗した操作を振り返る | 各試行の操作履歴を保存 | 採点JSONの `tasks[].attempts[].trace` |
| 自作モデルを学習させる | これから | 小型モデルで学習→保存→生成を動かす |
| 自分で改善案を試す | これから | 採点と隔離した実験環境を用意する |

「実機で確認」は、その課題で成功した記録です。いつでも正しく答えるという意味ではありません。

## 2026-09-16 — v0.2: 会話と調べもの

使用モデル: `qwen2.5-coder:3b`。追加学習や新しいモデルのダウンロードは行っていません。

### 試したこと

1. 「私の名前はハルです」と自己紹介し、挨拶してもらう。
2. 「私の名前は何でしたか？」と聞く。
3. TypeScriptについてネット検索し、日本語の説明と参照元を出してもらう。

### 途中で気づいたこと

- 最初はユーザーの名前と自分の名前を取り違えた。
- 会話モードから不要なコード編集の説明を除き、会話履歴を参照する指示を明確にした後、名前の確認に成功した。
- 検索では一時的な接続失敗もあった。接続切断だけ1回再試行し、認証画面やHTTPエラーは失敗として伝える形にした。
- 検索成功時には、実際の検索結果に含まれるTypeScript公式サイトを参照して回答できた。
- 小さなJSON編集では、モデルが引用符を落として置換に失敗する例もあった。元の内容は保持された。短い一意な文字列でも部分置換できることを、指示とエラー文に追加した。
- 調整後の練習課題では、ファイルの読み取り→正しい引用符で部分置換→読み直しまで成功し、無関係な設定も保持できた。

確認結果: モデルなしのテスト32件、型チェック、`check:live`、`check:code` が通過。実機確認はこのモデル・この課題での成功例として記録する。

これはエージェントの仕組みを改良した記録です。モデルの重みが育ったという意味ではありません。

## 2026-09-16 — v0.3: 記憶・感想・能力テスト

- 呼び方や話し方を明示して保存し、再起動後に参照・削除できるようになった。
- 回答への感想を保存し、次の関連質問に添える仕組みが入った。モデルが必ず指摘どおりに答えることまでは保証しない。
- 所在地や時刻表などの質問で、検索と必要な本文取得を先に行う処理が入った。
- `eval.ts` と10件の課題が追加され、繰り返し採点とモデル比較を行えるようになった。
- 保存済みの `3b-r5-desc-reverted.json` は項目追加0/5、1行変更4/5。`7b-r5.json` は項目追加4/5、1行変更5/5だった。モデル・一部の指示が異なる実験なので、純粋なモデルの能力差とは断定しない。

会話を覚える処理・ツールの判断・モデルの文章生成を分けて見ることが大切。所在地や時刻表は、実際の検索結果やサイトの状態でも成否が変わる。

## 2026-09-17 — v0.4: 設定の追加と失敗の記録

### 直した弱点

3BモデルにJSONへの項目追加を頼むと、`theme` を消して `language` に置き換えてしまった。
改善前の再計測でも3回とも同じ失敗だった。操作履歴を残したことで、原因を実際の引数まで追えるようになった。

### 変えたこと

- `edit_json` を追加。モデルはファイル名・最上位のキー名・新しい値を指定し、他の設定や数値表記の保持はプログラムが担当する。
- JSONが壊れている、キーが重複している、確認中にファイルが変わった、といった場合は書き込まない。
- 採点JSONに各試行の操作・回答・失敗理由・ツール一覧を保存する。課題ごとに途中保存する。
- 真偽値の変更・配列の追加という別の2課題も追加した。計12課題になった。
- 最初の版は項目追加2/3、1行変更1/3。値に余分な引用符を入れる失敗と、通常のコードをJSON風に引用してしまう失敗が残った。
- 値の渡し方を短い例で示し、JSONの専用ツールは依頼や読み取ったファイルから必要と分かったときだけ渡す形にした。

### 同じ3Bモデルで測った結果

モデル: `qwen2.5-coder:3b`。応答待ち上限300秒、各課題3回。既存課題の依頼文と合格条件は変更していない。

| 課題 | 改善前 | 最終版 |
|---|---:|---:|
| JSONの項目追加 | 0/3 | 3/3 |
| 通常コードの1行変更 | 2/3 | 1/3 |
| 真偽値の変更 | 未測定 | 3/3 |
| 配列の項目追加 | 未測定 | 3/3 |

JSONの3課題は合計9/9で成功。項目追加の平均所要時間は56.0秒から37.1秒になった。
通常コードの変更は、読み取った内容と違う置換文字列を繰り返す失敗が残った。今回の数値も下がっており、コーディング全体の改善は確認できていない。
各3回の小さな試行なので、成功率や速度がいつでも再現するとは限らない。

- [改善前の記録](results/before-json-tool-3b.json)
- [途中の版の記録](results/after-json-tool-3b.json)
- [最終版の記録](results/final-json-tool-3b.json)

モデルなしのテスト63件と型チェックが通過。練習用CLIの起動も確認した。
`npm run practice` で同梱の設定ファイルを使って試せる。追加後に元の `theme` が残っているか、自分でも確認してみる。

### 次の一歩

操作履歴から失敗例を選び、置換に失敗した後の手順を改善する。例えば「元の文字列を読み直し、使った誤った文字列に引きずられずにやり直せるか」を別のファイルでも採点する。
自動改善へ進むときは、この記録を入力に改善候補を作り、普段の作業場所とは別で評価する。合計点だけでなく、以前できた課題を壊していないことも採用条件にする。

これは既存モデルを支える道具の改善。モデルの重みの学習や、自動でコードを改修して採用する機能はまだ行っていない。

## 2026-09-17 — v0.5: 応答の順次表示と速度の計測

### 今回確認した状態

手元で検索判定の言い換えへの対応、判定用のtune / holdout課題、取得済みページや未確認URLへの取得を制限する処理が追加されていた。
保存済みの `routing-e2e-guarded-3b.json` では地名検索の3課題が合計8/9だった。これらを踏まえ、今回は応答が遅い理由を調べた。

### 遅かった理由と変えたこと

- このPCのCPUは Intel Core Ultra 5 125U。Ollamaの実行情報では3BモデルのGPU使用メモリは0だった。
- 以前は `stream: false` で、最後の文字ができるまで回答を一括で待っていた。
- 生成した文字から順に表示する方式を追加。ツール構文やコードが始まった場合は、その先の表示を完成まで保留する。
- 途中で切断した応答からツールを実行せず、その未完了応答を会話履歴へ追加しない。最終回答は全文を1件として残す。
- `/stats` で応答全体・最初の表示・モデル読み込み・入力処理・文章生成の時間を確認できるようにした。採点の操作履歴にもモデルの完了計測が残る。
- `npm run bench:response` で同じ短い質問を繰り返して測れるようにした。

### 実測

モデル: `qwen2.5-coder:3b`。挨拶・変数の説明・名前を含む自己紹介の3問を各2回、独立した会話として送信。指示は1559文字、モデル設定は同じ。

| 計測 | 改善前の一括表示 | 順次表示 |
|---|---:|---:|
| 最初の文字までの平均 | 4.38秒 | 0.42秒 |
| 回答完了までの平均 | 4.38秒 | 3.65秒 |

モデルへの入力後の計測であり、ネット検索や確認への返答待ちは含まない。各6回の小さな試行で、回答の長さも毎回違う。
主な改善は読み始められるまでの時間。モデルの計算自体が大幅に速くなった、という結果ではない。
長めの説明では文章生成に約7〜11秒かかり、ここが待ち時間の大部分だった。初回や入力キャッシュがない場合は、入力処理の待ち時間も増える。

- [改善前の記録](results/response-before-3b.json)
- [採用した順次表示の記録](results/response-stream-3b.json)
- [採用しなかった指示短縮の記録](results/response-fast-3b.json)

会話指示を813文字に短縮する実験も行ったが、「こんにちは、ハルです」とユーザーの自己紹介を自分のように返す応答が出たため採用しなかった。会話・調べもの・コード編集の指示は元の内容を使用する。

確認結果: 自動テスト99件・型チェックが通過。実機の会話・名前の確認・検索結果の回答と参照元提示も通過。小さなコード編集でも、ファイル読み取り→JSONの指定値だけ変更→読み直しを確認した。
CLIのテストでは、サーバーが最後のデータを送る前に画面に文字が表示されることを確認した。

### この後、賢くする順番

1. **資料を正しく読めるか測る。** 資料にある事実を答える課題と、資料に答えがないときに未確認と伝える課題を用意する。検索の成功と回答の正しさを別に採点する。
2. **失敗を再現できる課題へ変える。** `/feedback bad` の具体的な指摘から、期待する答え・残すべき設定・参照する出典を決めて能力テストへ追加する。感想を保存しただけでは重みの学習は進まない。
3. **一つずつ改善して、別の問題でも確かめる。** 同じ依頼の言い換えや別のファイルをholdoutにし、調整中には使わない。正確さと待ち時間の両方を比較する。
4. **候補作りと採点を自動化する。** 回数を決めて別フォルダで候補を試し、以前できた課題を壊した候補は採用しない。モデルの重みの学習は、こうして確認できたデータが集まってから別の実験として進める。

## 2026-09-19 — v0.7: コードを直す課題と、つまずきを受け止める骨格

モデル: `qwen2.5-coder:3b`（CPUのみ）。モデル・システムプロンプト・ツールの説明文は変えず、ツールとエージェントの処理だけを直した。

### 採点に足したこと

設定値の変更より一段むずかしい「コードを直す」課題を、tune と holdout に3件ずつ足した。holdout は tune と同じ種類で、ファイル・関数・言い回しだけを変えてある。

| tune | holdout | 内容 |
|---|---|---|
| fix-failing-test | holdout-fix-failing-test | `npm test` が落ちている。原因のコードを直す（テストは変えない） |
| fix-described-bug | holdout-fix-described-bug | 説明されたバグを直し、隣の関数を残す |
| add-function | holdout-add-function | 既存の関数を残して、新しい関数を足す |

採点はモデルの「直しました」を信じず、採点側がテストを走らせ、編集後のモジュールを読み込んで確かめる。テストに合わせて値を埋め込んだだけの修正を見分けるため、テストにない入力でも確かめる。
`npm run eval -- --holdout` は holdout だけを走らせ、合計だけを表示する。変更前の holdout も合計だけを見て、直している間は一度も走らせていない。

### 操作履歴で見つけた失敗と、変えたこと

| 見つけた失敗（変更前の tune） | 変えたこと |
|---|---|
| 「読む→直す→読み直す」の3回目の read_file を、行き詰まりと誤判定して打ち切った | ファイルを変えるツールが成功したら、繰り返しの数を数え直す |
| 使えない `web_search` を呼んだだけで調べものに切り替わり、作業ごと終わった | 使えるツールのときだけ調べものに切り替える |
| 複数行の関数を1行に詰めて引用し、空白が一致せず同じ呼び出しを繰り返した | 空白と改行を無視して1か所に絞れれば置換。ただし既存の定義を消す置換は拒否 |
| 関数を足すとき、3回中3回とも `old_string` を空にし「挿入はできません」で止まった | 空、または省略された `old_string` はファイル末尾への追加として受ける |
| 足した関数を二重に定義し、読み込めないファイルのまま「完了」と報告した | `.js` の書き込み後に `node --check`。壊した編集は元に戻し、すでにある定義の行を示す |
| 読んだ直後でも思い込みの `TIMEOUT_MS: 20_000` で置換を繰り返した | 見つからないとき、実物の近い行を JSON 文字列の形で示す |

### 同じ3Bモデルで測った結果

各課題3回。tune は保存済みの記録の合計。

| 版 | tune 全13課題 | うち「コードを直す」3課題 | holdout 3課題 |
|---|---:|---:|---:|
| 変更前 | 31/39 | 2/9 | 2/9 |
| 繰り返し判定・web_・空白・末尾追加 | 29/39 ※ | 2/9 | — |
| ＋構文確認と取り消し | 32/39 | 4/9 | — |
| ＋近い行・定義行の提示、省略時の補完 | **35/39** | **5/9** | 次回測定 |

※ 途中でPCがスタンバイに入り、2回が応答待ちの時間切れになった。モデルの失敗ではない。

- [変更前](results/v0.7-baseline-3b.json) / [holdout 変更前](results/v0.7-baseline-holdout-3b.json)
- [最終版](results/v0.7-hints-3b.json)（holdout の最終版は次回測定）

課題ごと（変更前 → 最終版）: fix-described-bug 2/3 → 3/3、add-function 0/3 → 2/3、preserve-rest 3/3 → 3/3（途中の版では 1/3 に揺れた）、fix-failing-test 0/3 → 0/3。既存の課題で下がったものはない。

### 分かったこと

- **モデルは賢くなっていない。変わったのは、つまずいた後に次の一手があるかどうか。** add-function で成功した回も、モデルは相変わらず最初に既存の関数を置き換えようとした。骨格が「定義が消える」と拒否して末尾への追加を案内したら、そこで正しい追加に移った。
- **失敗しても壊さない。** 最終版で add-function に失敗した1回は、定義を消す置換を1回、構文を壊す編集を4回試み、すべて拒否・取り消されてファイルは元のまま残った。以前は二重定義のまま「完了しました」と報告していた。
- **骨格で補えない部分がはっきりした。** fix-failing-test では、ループが1から始まっていることを見抜けない。最終版の3回のうち2回は、ファイルを読み直すだけで手が止まった。残る1回はテストを走らせて「50 !== 250」を読んだうえで、`+ 100` を足すような見当違いの編集をした。別の版では、禁止されていたテストファイルを書き換えた回もあった。止まる原因を取り除いても 0/3 のままで、ここは推論の力が要る。
- 途中の版で preserve-rest が 3/3 → 1/3 に揺れた。変更前と同じ入力で、読み直した後に思い込みの文字列を繰り返すかどうかが分かれた。n=3 では揺れと悪化を区別できない。その失敗の形を直接受け止める変更（近い行の提示）を入れた後は 3/3 で、3回とも提示を見て正しい行を引用した。
- 作業中のスリープ防止が、起動の仕方の誤りで効いておらず、夜間の採点が7時間止まった。効いているかをログで確かめるようにした。

### 次の一歩

1. **推論が要る課題を、骨格の側からも試す。** 例えば「編集→テスト→通らなければ元に戻し、失敗の内容を添えて別の案を求める」を自動化し、試せる案の数を増やす。
2. **同じ課題を 7B でも測る。** 骨格の改善とモデルの大きさ、それぞれで何が変わるかを分けて記録する。
3. **自作モデルへの足がかり。** 骨格が受け止めた失敗（拒否・取り消し・近い行の提示）は、そのまま「小さいモデルが間違えやすい形」のデータになる。`trace` に残っているので、将来の学習データや評価課題の元にできる。

## 2026-09-23 — 失敗テストの再確認をエージェントに追加

失敗テストの修正依頼では、`package.json` に `test` スクリプトがあれば先に実行する。出力に現れたテストファイルと、その相対import先の実装ファイルを読んでからモデルに渡す。単独のテストコマンドが失敗した後は、そのコマンドを覚え、編集後に再実行する。再実行にもユーザーの確認を通す。通らなければ結果をモデルに返して別の修正を促し、未通過の「完了」報告を止める。シェルの複合コマンドや式展開を含む入力は自動再実行しない。

モデルなしの結合テストで、先行するテスト・ファイルの読み取り、編集後の成功、失敗が続く場合の報告、複合コマンドを再実行しないことを確認した。Ollama のコマンドは PATH にないがローカルAPIには接続できたため、評価用の一時的な `npm test` 入口を使い、同じ `fix-failing-test` を各モデルで1回試した。

| モデル | 結果 | 所要時間 | 操作履歴 |
|---|---:|---:|---|
| `qwen2.5-coder:3b` | 失敗 0/1 | 165.2秒 | テストと実装を読んだが、ループ本体を誤編集。再テストの失敗は正しく検出した。 |
| `qwen2.5-coder:7b` | 成功 1/1 | 265.1秒 | ループを配列の各要素で回す形へ修正。1回目の編集は失敗したが、再テスト後の2回目で通った。 |

- [3Bの記録](results/2026-09-23-fix-failing-test-context-3b.json) / [7Bの記録](results/2026-09-23-fix-failing-test-context-7b.json)

途中段階の3B試行では、[テストを先に走らせる前](results/2026-09-23-fix-failing-test-3b.json)はテストファイルのパスを推測して停止し、[テストだけ先に読んだ段階](results/2026-09-23-fix-failing-test-guided-3b.json)ではテストのassert文を実装側のコードと取り違えた。両方の実物を渡した後は、誤った編集でも再テストと未完了の報告まで進めた。

各1回なので、モデル間の安定した成功率の比較ではない。7Bの最終回答には、実際には行っていない「コーディングモードから退出」という誤報があった。ファイルの採点は最終文面ではなく、テストファイルの保持、テストの成功、別入力での関数の動作で判定した。

続いて「テストファイルは変更しないで」という指示を編集ツール側でも守る関門を加えた。モデルがテストファイルへの編集を試みても実行せず、実装側を直すよう伝える結合テストが通った。

別の失敗テスト課題を7Bで1回だけholdout評価し、**1/1成功、176.5秒**だった。[結果](results/2026-09-23-holdout-failing-test-7b.json)。課題の操作履歴は改善には使わず、holdoutのまま保持した。2種類とも1回ずつの成功であり、幅広いコード修正に通用するとの結論はまだ出せない。

次は独立したコード修正課題を増やし、失敗パターンごとの成功率と所要時間を測る。

## 2026-09-23 — 失敗テストの修正だけ7Bへ切り替える

自動モデル選択時に `qwen2.5-coder:3b` と `qwen2.5-coder:7b` が共に導入済みなら、通常の応答には3Bを使い、コードモードで失敗テストの修正を頼まれた実行に限って7Bを使う。切り替えを画面に表示し、使うモデルの文脈上限で履歴を管理する。7Bが無い場合と、モデルを明示した場合は切り替えない。このPCのOllama APIで両モデルが導入済みと確認した。

モデルなしのテストで、通常依頼・失敗テスト・テスト実行禁止・会話モード・明示的なモデル変更の選択を確認した。全135テストと型チェックが通過した。7Bによる実際の修正成功は上記の各1回の記録に限られ、今回の切り替え追加で成功率が上がったと評価するには新たな複数課題での測定が必要。

## 2026-09-23 — 調べものの回答が時間切れで消える問題を修正

市の公式ページを取得した後、3Bモデルの要約が120秒で時間切れとなり、画面にはエラーだけが出て回答が返らない例があった。資料から答える段階の生成上限を1024から256トークン、既定の待ち時間を120秒から60秒に変更した。モデルが時間切れになっても、取得済みのページや検索結果に関連文があれば、その文を引用して出典を付ける。資料がない場合は推測で埋めない。「どんな場所？」は所在地だけでなく、概要と特徴を聞く質問として扱う。

同種の質問と模擬の公式ページを使い、このPCの3Bモデルから約13〜25秒で出典付きの回答を得た。回答の後に無関係な「確認できませんでした」を足す例もあったため、その独立した行は実質的な回答が先にある場合に限って除く。時間切れを模擬した結合テストでは、取得済み資料から引用回答が返ることを確認した。全138テストと型チェックが通過した。実際のページでの速度は、Ollamaの混雑やPCの負荷によって変わる。

## 2026-09-23 — ローカル7Bの追加課題と応答待ちの改善（v0.8.2）

ユーザーの希望に合わせ、推論は完全ローカルのままとした。GPT-5 APIや新しいモデルのダウンロードは追加していない。このPCの7BはCPU実行で遅いため、失敗テストの修正以外にも自動適用してよいか、まず関数追加で確認した。

| 条件 | 関数追加の結果 | 所要時間 |
|---|---:|---:|
| 変更前の7B | 0/1 | 159.7秒 |
| 編集エラーを具体化した後の同じ課題 | 1/1 | 157.4秒 |
| 別の関数追加課題（holdout） | 1/1 | 147.5秒 |

変更前は、7Bが既存の `subtract` を `multiply` に置き換える案を出し、さらに改行を2文字の `\\n` として渡した。`edit_file` は一致せず安全に拒否したが、モデルは同じ編集を繰り返して停止した。エラーに「改行の二重エスケープ」と「既存の定義が消える」こと、および末尾追加の方法を示すと、再試行では `old_string` を空にして正しく追加し、元の関数も残した。[変更前](results/2026-09-23-add-function-7b.json) / [変更後](results/2026-09-23-add-function-7b-after.json) / [holdout](results/2026-09-23-holdout-add-function-7b.json)。holdout の内訳は改善に使用していない。

モデルから30秒間文字が届かない場合、使用モデルと経過時間を画面に出す。モデルの時間切れは例外だけで終わらせず、作業が未完了であることを回答として返す。コード編集の成否は引き続き実ファイルとテストで判定する。140テストと型チェックが通過した。

この3回だけでGPT-5相当の能力や7Bの安定した成功率は主張できない。7Bは関数追加でも約2分半かかるので、自動選択は引き続き失敗テスト修正に限定する。

## 2026-09-23 — 自己改修の候補生成を隔離（v0.9.0）

完全ローカルのまま、`npm run self:propose` で origin-agent 自身の修正案を作れるようにした。クリーンなHEADから一時的なGit作業コピーを作り、モデルには読み取りと `src/` の既存ファイルの編集だけを許す。シェル・Web・テストファイル・採点器・設定の編集は許さない。複数回の独立した候補、総時間の上限、Windowsで実行中だけのスリープ防止を用意した。差分は `.origin-agent/proposals/` に未検証のパッチとして残し、元の作業ツリーに適用しない。

候補コードを実行・採点する隔離環境はこのPCにまだない。DockerとWSLの実行環境を確認できなかったため、候補を無人でテストしたり採用したりする処理は追加していない。今回の機能でモデル自体の重みは変わらない。モデルなしの結合テストで `src/` 以外の編集拒否と `src/` の編集を確認し、全141テストと型チェックが通過した。

このPCの3Bモデルで1回試したところ、約100秒で差分を作らず終了した。一時作業コピーは削除され、元の `main` はクリーンなままだった。差分がない試行も操作履歴を保存するようにした。
対象を絞った2回目は120秒で最初の応答が時間切れになった。長いコード文脈をCPUだけで読む場合の制限が明確になったため、候補生成の1回の応答上限を最大300秒（総時間の上限が先に来ればそこまで）にした。

次は、ローカルの隔離環境で候補コードを実行し、固定の課題と独立した課題の両方で比較する。候補が悪化したら破棄し、採用できる場合も検証記録と差分を残す。

## 2026-09-24 — 自己改修案のローカル隔離評価（v0.10.0）

`self:propose` の記録に生成時のGitコミットIDを保存するようにした。`self:evaluate` は、そのコミットから一時作業コピーを作り、パッチの適用可否と変更対象を確認する。評価器と編集制限の実装を除く `src/` 内の既存の通常ファイル1〜5件以外は候補として実行しない。固定テストと型チェックを、基準版と候補版で同じローカルDockerイメージ内で走らせる。結果にはパッチのSHA-256、変更ファイル、両方の出力、判定を記録する。候補は元の作業ツリーへ適用しない。

Docker実行では、Gitを含む `node:24-bookworm` のローカルイメージだけを使い、自動取得しない。ネットワークなし、読み取り専用の作業コピーと依存ライブラリ、非rootユーザー、権限削除、CPU・メモリ・プロセス数・時間の制限を設定する。Dockerが使えない場合は評価前に失敗する。このPCにはDockerが無いため、実際のコンテナでの動作は未確認。Git作業コピーと模擬の評価器による結合テストで、候補の比較、元の作業ツリーの保持、テストファイル変更の拒否を確認した。

固定テストの通過だけで改善とは判断しない。基準版からのテスト成績の変化を表示するだけであり、今後は独立した課題での性能評価、差分レビュー、候補の安全な取り込み手順が必要。現段階では自動採用・プッシュをしない。

## 2026-09-24 — 保存済み評価のレビューと自己改修評価の整合性

`eval:review` で、Ollamaなしに保存した評価の失敗種別・最後の質問と回答・成功失敗別の所要時間を確認できるようにした。
今後の測定ではモデルの時間切れ・生成エラーを記録し、Agentが未完了の説明を返した場合も通常回答と混同しない。
新しい結果にはGitコミット、未コミット変更の有無、Nodeのバージョンを保存する。旧形式は欠けた情報を推測しない。
holdoutは合計だけを表示し、途中結果・異なる測定条件に注意を出す。

自己改修案は開始時のコミットに固定して生成する。隔離評価では、読み取ってハッシュ化したパッチをメモリ上の同じバイト列から適用する。
許可外パッチを検査実行前に拒否し、TAP集計・型チェック終了・基準版とのテスト件数を確認する。未完了やスキップは改善とせず `invalid_checks` とする。
Linuxの非root実行ではホストのUID/GIDを使い、一時コピーを他ユーザーへ公開することなく読めるようにした。

検証: 型チェック、モデルなしの自動テスト167件、既存の保存済み評価の読み取り、固定検査コマンドとTAP集計のLinuxホスト実行が成功。
Dockerと実モデルはこの開発環境にないため、実コンテナの動作や会話品質の改善は未測定。クラウド契約・モデルのダウンロードは行っていない。
テスト完了の確認は出力偽装への完全な防御ではなく、独立した能力評価・人の差分確認・採用判断は依然として必要。

## 次に育てたいこと

### 会話・エージェント側

- [x] 好きな呼び方や話し方を、明示して保存・確認・削除できる。
- [x] 「うまくできた」「ここが違った」を記録できる。
- [x] 会話・調べもの・小さな編集について、10件程度の能力テストを持つ。
- [x] 過去の経験から関連するものだけを探して、次の作業に使う。
- [ ] 指示文や作業手順の候補を比較し、よくなったものを採用する。

### モデル側

- [ ] 文字を数値に変換し、小型モデルで次の文字を予測する。
- [ ] 学習を保存・再開し、短い文章を生成する。
- [ ] 学習に使っていないデータで成績を測る。
- [ ] 簡単な指示からツール呼び出しを出す課題に挑戦する。
- [ ] `Provider` でつなぎ、同じ課題を既存モデルと比較する。

### 自動改善側

- [ ] 実験回数・時間・使う資源を制限できる。
- [ ] 普段の作業場所から隔離した環境で候補を試せる。
- [ ] 改善するAIとは別に、採点プログラムと合格基準を管理する。
- [ ] 改善に使っていない課題でも確認し、以前の版へ戻せる。

## 記録のテンプレート

```text
日付:
モデル・設定:
試したこと:
期待した結果:
実際の結果:
変えたこと:
同じ課題で再確認した結果:
次に試したいこと:
```
